// wrap original component and its children into this component
export const WRAP_WITH = 'wrapWith';

// use this component instead of original and parse/place children inside this component
export const USE_AS_ROOT = 'useAsRoot';

// use this component instead of original, don't parse/place children inside
export const USE_INSTEAD = 'useInstead';

export const STORE_NAME = 'FigmaToReactStore';
export const TEXT_STORE_NAME = 'JsonStore';
